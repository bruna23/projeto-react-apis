import styled from "styled-components";

export const Container = styled.div`
  section {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 40px;
    background: #5e5e5e;
    padding: 50px;
    width: 1200px;
    h1 {
      width: 100%;
      height: 72px;
      left: 60px;
      top: 220px;

      font-family: "Poppins";
      font-style: normal;
      font-weight: 700;
      font-size: 48px;
      line-height: 72px;

      color: #ffffff;
    }
  }
`;
